class EditableListPromiseResponse {
    constructor(
        isSuccess,
        payload
    ) {
        this.isSuccess = isSuccess;
        this.payload = payload;
    }
}

export default EditableListPromiseResponse;